// This is the entry point for Cypress plugins

// You can use plugins to customize Cypress behavior
// and inject additional functionality

// Read more about plugins here: https://on.cypress.io/plugins-guide

module.exports = (on, config) => {
  // Add your custom plugins here

  // Return the updated Cypress config
  return config;
};